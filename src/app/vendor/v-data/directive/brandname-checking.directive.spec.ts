import { BrandnameCheckingDirective } from './brandname-checking.directive';

describe('BrandnameCheckingDirective', () => {
  it('should create an instance', () => {
    const directive = new BrandnameCheckingDirective();
    expect(directive).toBeTruthy();
  });
});
